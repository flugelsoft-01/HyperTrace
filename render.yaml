services:
  - type: web
    name: hypertrace
    env: docker
    dockerfilePath: ./Dockerfile
    region: singapore # or oregon, frankfurt, ohio
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_ENV
        value: production
